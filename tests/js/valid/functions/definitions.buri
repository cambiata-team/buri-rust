-- $$only

@export
add = (a, b) => a + b

@export
multilineAdd = (a, b) =>
    a + b

@export
ifTrueFiveElseThree = (a) => if a do 5 else 3

@export
person = { name: "John", age: 30 }

@export
getName = (record) => record.name

@export
getAge = (record) => record.age